import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const MainCourses = ({route}) => {
  console.log(route);

  return (
    <View>
      <Text>Main Courses</Text>
    </View>
  )
}

export default MainCourses

const styles = StyleSheet.create({})