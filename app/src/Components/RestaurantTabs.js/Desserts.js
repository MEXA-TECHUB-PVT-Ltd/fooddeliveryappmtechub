import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Desserts = ({route}) => {
  console.log(route);

  return (
    <View>
      <Text>Desserts</Text>
    </View>
  )
}

export default Desserts

const styles = StyleSheet.create({})